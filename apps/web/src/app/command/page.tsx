import { InternalCommandCenter } from "@/components/InternalCommandCenter";

export default function CommandCenterPage() {
  return (
    <main className="app-shell cockpit-shell">
      <InternalCommandCenter />
    </main>
  );
}
